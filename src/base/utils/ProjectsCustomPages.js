/**
 * Custom pages projects
 * @param conf
 */
export const ProjectsCustomPages = (conf) => {
    return {
        projectPersonalSite: {
            id: 1,
            route: conf.routes.ps.projectPersonalSite
        }
    }
};