import * as React from 'react';
import {Container, Typography} from "@mui/material";

function PageBlogList() {
    return (
        <Container maxWidth="lg" className={"Page BlogList"}>
            <Typography align={"center"} variant="h2">
                Page Blog List
            </Typography>
        </Container>
    );
}

export default PageBlogList;
