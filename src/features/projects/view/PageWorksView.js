import * as React from 'react';
import {Container} from "@mui/material";

function PageWorksView() {
    return (
        <Container maxWidth="lg" className={"Page WorksView"}>
            PageWorksView
        </Container>
    );
}

export default PageWorksView;
