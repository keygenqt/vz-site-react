import * as React from 'react';
import {Container, Typography} from "@mui/material";

function PageWorksList() {
    return (
        <Container maxWidth="lg" className={"Page WorksList"}>
            <Typography align={"center"} variant="h2">
                Page Works List
            </Typography>
        </Container>
    );
}

export default PageWorksList;
